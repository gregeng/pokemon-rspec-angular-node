testing an angular app with a node backend

- uses chrome driver for feature tests
- clears db between test runs
- exposes object creation methods for end to end testing
- includes capybara for feature tests
